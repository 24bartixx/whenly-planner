import 'package:flutter/widgets.dart';

class BrowseWorkSessionsView extends StatelessWidget {
  const BrowseWorkSessionsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Browse sessions"));
  }
}
